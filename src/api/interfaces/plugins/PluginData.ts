// plugin data stored in the backend
interface PluginData {
	id: string;
	name: string;
	description: string;
	favoritesCount: number;
    imageUrl: string;
    packageUrl: string;
}

export {
	PluginData
};
